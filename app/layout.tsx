import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Café de la Seine',
  description: 'Create an engaging landing page for a Parisian coffee shop to attract local customers and tourists, showcase the authentic French coffee experience, and drive foot traffic to the physical location',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}